# Motor + HMI are now simulated inside AERA on a shared real-time state.
# Optional LLM — used only to rephrase AERA's already-computed assessment.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_BASE_URL=https://api.openai.com/v1

AERA_HOST=127.0.0.1
AERA_PORT=8001
AERA_CORS_ORIGINS=http://localhost:3001,http://127.0.0.1:3001
